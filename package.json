{
  "name": "digesto",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "turbo run start",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "coverage": "turbo run coverage"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
